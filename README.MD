# PDF 연결 구획
1.웹에 PDF 파일이 업로드되면 웹 -> Java 서버로 PDF파일 전송

2.Java 서버에서 PDF 파일을 즉시 C++ 클라이언트로 전송

3.MuPDF 라이브러리를 활용한다.

4.각 C++ 클라이언트에서 캔버스의 레이어를 현재 PDF 이미지(jpg), 실제 그림 작업대 로 나눈다.

5.웹에서 페이지 전환 발생 시 Index 정수만 서버로 보내 클라이언트로 전송하여 JPG 교체를 진행한다.


기타 개발 계획 추천 :
- 교수자가 세션코드를 생성 -> 학생이 세션코드로 참가
-
-
-
